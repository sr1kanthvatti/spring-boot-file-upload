# spring-boot-file-uplod
Added all dependencies Spring boot dependencies in pom.xml and  no need extra library for file upload.
Once application build succesfully, you can access the application using url http://localhost:8080/
Select file and upload it
